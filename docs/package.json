{
  "name": "docs",
  "type": "module",
  "version": "1.0.0-beta.25",
  "private": true,
  "scripts": {
    "dev": "astro dev --force",
    "build": "astro build",
    "preview": "astro preview",
    "predev": "pnpm run generate-reference",
    "prebuild": "pnpm run generate-reference",
    "prepare": "astro sync",
    "generate-reference": "jiti ./scripts/generate-reference.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserslist": [
    "fully supports css-cascade-layers"
  ],
  "dependencies": {
    "astro": "catalog:build",
    "astro-friday": "workspace:*",
    "typedoc": "catalog:docs",
    "typedoc-plugin-frontmatter": "catalog:docs",
    "typedoc-plugin-markdown": "catalog:docs"
  }
}
